<?php

namespace Tests;

use PHPUnit\Framework\TestCase as BaseTestCase;

/**
 * ExampleTest — a minimal PHPUnit test for a generated ZeroPing application.
 *
 * Run your suite with `composer test` (or `phpunit`).
 */
class ExampleTest extends BaseTestCase
{
    /**
     * A trivial sanity check that the test runner is wired up.
     */
    public function testItWorks(): void
    {
        $this->assertTrue(true);
    }
}
