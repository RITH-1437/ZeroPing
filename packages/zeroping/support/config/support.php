<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Support package configuration
    |--------------------------------------------------------------------------
    |
    | The support package is foundational; it exposes no user-facing features
    | on its own. This file exists so packages can read shared defaults and so
    | the base provider demonstrates config merging.
    |
    */

    'auto_discover' => env('PACKAGE_AUTO_DISCOVER', true),
];
